pipeline {
    agent any 
    when {
        expression {
            BRANCH_NAME == 'master'
        }
    }    
    stages {
       stage ('Checkout') {
         steps {
            echo "Sit"
         }
       }
    }
}    
    
